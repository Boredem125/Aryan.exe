# Groq — server-side only. NEVER prefix with NEXT_PUBLIC_.
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile

# HMAC secret for signing visitor progress tokens.
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
PROGRESS_SECRET=
